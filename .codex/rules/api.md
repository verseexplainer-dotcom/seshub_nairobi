# API Rules

- Only use `src/pages/api/*` for API routes.
- Do not duplicate endpoints when one already exists.
- Do not expose the service role key to the client.
- Keep server-side writes on trusted routes only.
