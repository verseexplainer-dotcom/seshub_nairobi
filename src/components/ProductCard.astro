---
import type { CatalogProduct } from '../types/catalog';
import SharedProductCard from './ui/ProductCard.astro';

interface Props {
  product: Record<string, any>;
}

const props = Astro.props as Props & { product: CatalogProduct };
---

<SharedProductCard {...props} />
