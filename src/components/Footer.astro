---
import { Image } from 'astro:assets';
import { buildImageOnError, getSiteAssets } from '../lib/siteAssets';
import { footerContactLinks, mainShopLinks, paymentMethods, storefrontDetails, supportLinks } from '../lib/storefront';

const year = new Date().getFullYear();
const siteAssets = getSiteAssets(import.meta.env.PUBLIC_SUPABASE_URL);
const logoImage = siteAssets.logo;
const logoOnError = buildImageOnError(logoImage.fallback);
const logoWidth = 96;
const logoHeight = 96;
const companyLinks = [
  { label: 'Blog', href: '/blog' },
  { label: 'Contact', href: '/contact' },
  { label: 'Track Order', href: '/track' },
  { label: 'FAQ', href: '/faq' }
];
---

<footer class="site-footer">
  <div class="container footer-top">
    <section class="footer-brand">
      <a href="/" class="footer-logo" aria-label="SES ICT HUB home">
        <Image
          src={logoImage.src}
          alt="SES ICT HUB logo"
          width={logoWidth}
          height={logoHeight}
          class="footer-logo-image"
          loading="lazy"
          decoding="async"
          onerror={logoOnError}
        />
      </a>

      <div class="footer-brand-copy">
        <h2>{storefrontDetails.brandName}</h2>
        <p>
          Clear prices, honest stock updates, and help from our team when you need it.
        </p>
      </div>

      <div class="footer-socials" aria-label="Contact and social actions">
        {footerContactLinks.map((link) => (
          <a href={link.href} class={`footer-social-link ${link.tone || 'neutral'}`}>{link.label}</a>
        ))}
      </div>

      <div class="footer-payments" aria-label="Payment methods">
        {paymentMethods.map((method) => (
          <span class="footer-payment-pill">{method}</span>
        ))}
      </div>
    </section>

    <div class="footer-links-grid">
      <nav class="footer-column" aria-labelledby="footer-shop-title">
        <h3 id="footer-shop-title">Shop</h3>
        <ul role="list">
          {mainShopLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
          <li><a href="/shop">Browse Shop</a></li>
          <li><a href="/shop?sort=featured">Featured</a></li>
        </ul>
      </nav>

      <nav class="footer-column" aria-labelledby="footer-support-title">
        <h3 id="footer-support-title">Support</h3>
        <ul role="list">
          {supportLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
          <li><a href="/cart">Cart</a></li>
          <li><a href="/auth/login">Sign In</a></li>
        </ul>
      </nav>

      <nav class="footer-column" aria-labelledby="footer-company-title">
        <h3 id="footer-company-title">Company</h3>
        <ul role="list">
          {companyLinks.map((link) => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>
  </div>

  <div class="container footer-bottom">
    <div class="footer-contact-stack">
      <p>{storefrontDetails.fullLocation}</p>
      <div class="footer-contact-inline">
        <a href={`tel:${storefrontDetails.phone.replace(/\s+/g, '')}`}>{storefrontDetails.phone}</a>
        <a href={`mailto:${storefrontDetails.email}`}>{storefrontDetails.email}</a>
      </div>
    </div>

    <div class="footer-legal">
      <p>&copy; {year} {storefrontDetails.brandName}. All rights reserved.</p>
      <p>{storefrontDetails.deliverySummary}</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: clamp(4rem, 7vw, 6rem);
    padding-top: clamp(2.6rem, 5vw, 4rem);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    background: linear-gradient(180deg, #061633, #030712 72%);
    color: #ffffff;
  }

  .footer-top {
    display: grid;
    gap: 2rem;
    padding-bottom: 2rem;
  }

  .footer-brand {
    display: grid;
    gap: 1rem;
  }

  .footer-logo {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: clamp(5.2rem, 18vw, 6.8rem);
    height: clamp(3.35rem, 11vw, 4.35rem);
    padding: 0.18rem 0.4rem;
    border-radius: 1.2rem;
    border: 1px solid rgba(255, 255, 255, 0.16);
    background: rgba(255, 255, 255, 0.94);
    box-shadow: 0 18px 42px rgba(0, 0, 0, 0.24);
    overflow: hidden;
  }

  .footer-logo-image {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
    transform: scale(2.45);
    transform-origin: center;
  }

  .footer-brand-copy {
    display: grid;
    gap: 0.7rem;
    max-width: 34rem;
  }

  .footer-brand-copy h2 {
    color: #ffffff;
    font-size: clamp(1.45rem, 3vw, 1.85rem);
    letter-spacing: -0.04em;
  }

  .footer-brand-copy p {
    color: rgba(226, 232, 240, 0.76);
  }

  .footer-socials,
  .footer-payments,
  .footer-contact-inline {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
  }

  .footer-social-link,
  .footer-payment-pill {
    display: inline-flex;
    align-items: center;
    min-height: 2.5rem;
    padding: 0.55rem 0.9rem;
    border-radius: 999px;
    font-size: 0.88rem;
    font-weight: 800;
  }

  .footer-social-link {
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.08);
    color: rgba(255, 255, 255, 0.86);
  }

  .footer-social-link.whatsapp {
    border-color: rgba(37, 211, 102, 0.35);
    color: #b6f5d4;
  }

  .footer-payment-pill {
    background: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.88);
  }

  .footer-links-grid {
    display: grid;
    gap: 1.5rem;
  }

  .footer-column {
    display: grid;
    gap: 0.85rem;
    align-content: start;
  }

  .footer-column h3 {
    color: #ffffff;
    font-size: 0.96rem;
  }

  .footer-column ul {
    display: grid;
    gap: 0.7rem;
    padding: 0;
    list-style: none;
  }

  .footer-column a,
  .footer-contact-stack p,
  .footer-contact-inline a,
  .footer-legal p {
    color: rgba(226, 232, 240, 0.72);
  }

  .footer-column a:hover,
  .footer-contact-inline a:hover {
    color: #ffffff;
  }

  .footer-bottom {
    display: grid;
    gap: 1rem;
    padding-block: 1rem 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
  }

  .footer-contact-stack {
    display: grid;
    gap: 0.6rem;
  }

  .footer-legal {
    display: grid;
    gap: 0.35rem;
  }

  @media (min-width: 860px) {
    .footer-top {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
      align-items: start;
    }

    .footer-links-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (min-width: 1080px) {
    .footer-bottom {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      justify-content: space-between;
    }

    .footer-legal {
      text-align: right;
    }
  }
</style>
